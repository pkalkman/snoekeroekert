{
  "name": "snoekeroekert",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:public": "pnpm --filter snoek-admin dev",
    "dev:admin": "pnpm --filter snoek-public dev",
    "format": "prettier --write .",
    "check-format": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "5.8.3"
  }
}
